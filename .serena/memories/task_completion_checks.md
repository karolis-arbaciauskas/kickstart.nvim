After changing this Neovim config, at minimum verify that Neovim starts cleanly with `nvim --headless '+qall'`. For plugin or tool changes, also check `:Lazy`, `:Mason`, and `:checkhealth` inside Neovim. For Lua formatting changes, keep files compatible with `.stylua.toml` and run Stylua if available. Because this repo is a Kickstart fork with upstream tracking, consider whether local edits should preserve or intentionally diverge from upstream Kickstart structure.